<%= profile_img(@user) %>
<%= @user.name %>


<h3 class="text-center"><参加中のプラン></h3>
  <div class="container-fluid">
  	<div class="row">
  		<% @my_plans.each do |mp| %>
  		<div class="col-md-3 col-xs-offset-1">

  			<div class="media">
  				<a class="media-left" href="#">
  					<%= image_tag("/Prefectures/#{mp.area.id}.jpg", :size => "200x200", :alt => "アイコン", class: 'img-responsive') %>
  				</a>
  				<div class="media-rigth">
  					<h4 class="media-heading">
  						<%=link_to mp.name, plan_path(mp)%>
  					</h4>
  					メンバー (<%= mp.plan_users.count %>)
  				</div>
  			</div>
  		</div>
  		<% end %>
  	</div>
  </div>
